import os

def generate_qr_info(data):
    # This simulates a QR metadata generator
    print(f"Generating QR code for: {data}")
    print("QR Code successfully created and saved.")

if __name__ == "__main__":
    link = "https://github.com/yourusername"
    generate_qr_info(link)
